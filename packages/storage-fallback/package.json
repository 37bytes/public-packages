{
  "name": "@37bytes/storage-fallback",
  "version": "1.0.1",
  "description": "Safe fallbacks/wrappers for some storages (localStorage, sessionStorage)",
  "main": "./build/index.js",
  "scripts": {
    "build": "rimraf build && ./node_modules/.bin/tsc --project ./tsconfig.json",
    "build&watch": "rimraf build && ./node_modules/.bin/tsc --project ./tsconfig.json --watch",
    "npm-packlist": "npm run build && npx -y npm-packlist"
  },
  "keywords": [],
  "author": "tad-nikolaeva",
  "license": "MIT",
  "devDependencies": {
    "rimraf": "3.0.2",
    "typescript": "^4.6.4"
  }
}
